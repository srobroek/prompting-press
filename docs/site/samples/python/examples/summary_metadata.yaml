name: summary
role: user
body: "Summarise {{ article }}."
variables:
  article:
    type: string
    trusted: false
metadata:
  model_hint: claude-sonnet-4-6
  max_tokens: 512
  owner: team-content
variants:
  terse:
    body: "TL;DR of {{ article }}."
    metadata:
      weight: 0.2
      group: experiment-q4
