{
  "name": "@prompting-press-docs/samples-typescript",
  "version": "0.0.0",
  "description": "Doc-sample standalone programs (spec 014). Each file under examples/ is displayed verbatim in the docs via ?raw and compiled + run by the test harness.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json --noEmit && node --test \"examples/**/*.ts\""
  },
  "dependencies": {
    "prompting-press": "file:../../../../packages/typescript",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "tsx": "^4.19.2",
    "typescript": "^5.6.0"
  }
}
