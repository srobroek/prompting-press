{
  "name": "agreement-purity",
  "role": "user",
  "body": "{{ alpha }} and {{ beta.gamma }} {% for x in delta %}{{ x }}{% endfor %}",
  "variables": {
    "alpha": {
      "type": "string",
      "origin": "trusted",
      "description": "A root."
    },
    "beta": {
      "type": "object",
      "origin": "untrusted",
      "description": "A root with a nested field."
    },
    "delta": {
      "type": "array",
      "origin": "external",
      "description": "An iterated root."
    }
  }
}
