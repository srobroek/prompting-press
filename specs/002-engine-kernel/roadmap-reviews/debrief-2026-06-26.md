# Roadmap Review — Post-Implementation Debrief: Spec 002 (Engine kernel)

**Date**: 2026-06-26 · **Kind**: Post-Implementation Debrief · **Ledger entry**: 002 — Engine kernel
(`prompting-press-core`), currently `[status: planned]` · **Read-only** (proposes, does not apply).

## Surfaced Context

- **Entry outcome**: "given already-validated values + a prompt definition, renders text, reports
  required root variables, resolves variants, emits `template_hash`/`render_hash`, supports the opt-in
  guard expansion — no FFI, no typed-Vars knowledge."
- **Scope (in)**: MiniJinja restricted to interpolation/conditionals/loops; render path;
  `undeclared_variables(nested=false)` + globals allowlist; variant resolution (implicit/explicit
  default per C-05); `template_hash`+`render_hash`; 3-way provenance plumbing + configurable additive
  guard expansion; small engine-regression render fixtures.
- **Scope (out)**: includes/imports/extends, macros, inheritance; any FFI; any typed-Vars validation.
- **Governed by**: C-01, C-03, C-04, C-05, C-09.
- **Implementation**: branch `002-engine-kernel`, 14 commits, `crates/prompting-press-core/` (6 src
  modules, 50 tests), 7/7 CI gates green; verify = 100% FR/SC, security = LOW.

## Findings

| ID | Type | Severity | Detail | Recommendation |
|----|------|----------|--------|----------------|
| D1 | roadmap-stale | 💡 | Scope(in) says "variant resolution (implicit/**explicit** default per C-05)". The impl has implicit default (root `body` is always the default) but the *explicit-default* path was removed via the in-cycle FR-010 refine — it was structurally unreachable given the 001 schema (`body` required = always the default). Implementation is CORRECT; the roadmap phrasing predates the resolved 001 schema. | Amend the entry's scope wording to "implicit default (root body); caller-named variant selection; unknown-variant the only error" via `/speckit.roadmap.write`. Not an implementation defect. |
| D2 | roadmap-stale | 🤔 | Notes say "verified against MiniJinja 2.21 source." Confirmed accurate this cycle (re-verified against the 2.21.0 tag), but the entry could note the version is now *pinned* (`2.21`, default-features=false, macros/multi_template off) as the implemented mechanism. | Optional: fold the pinned-feature-set detail into Notes at `roadmap.write` time. |
| — | outcome-miss | — | None. Every outcome clause delivered: render ✅, required-roots ✅, variant resolution ✅, both hashes ✅, opt-in guard ✅, no FFI ✅ (check-ffi green), no typed-Vars knowledge ✅ (validation-blind). |
| — | scope-creep | — | None. Excluded features rejected (SC-008); no FFI; no typed-Vars validation. The cargo-deny advisory gate (T036) is a security CI guardrail, not release tooling — assessed in sync-conflicts J2 as in-lane, not creep into 007/C-10. |
| — | constraint-violation | — | None. C-01 (structural parity/determinism), C-03 (minimal boundary, no I/O), C-04 (sound agreement check), C-05 (no vars_hash, hashes over strings), C-09 (provenance metadata + opt-in guard, no sanitization) all satisfied — confirmed by the plan Constitution Check + both security reviews. |

## Findings Summary

2 findings, both **roadmap-stale** (the roadmap phrasing trails the resolved design; the implementation
is correct). 0 outcome-miss, 0 scope-creep, 0 constraint-violation. No 🎯 Must-Address.

## Verdict

✅ **PROCEED** — the implementation fully delivers the entry's outcome within scope and under all
governing constraints. The two findings are stale *roadmap wording*, not implementation defects.

## Recommended Actions

1. **Propose status transition `planned → implemented`** — the outcome is met and there are zero
   🎯 Must-Address findings. (Mirrors how 001 moved to `implemented` after its SC gates; 002 would move
   to `verified` after this debrief + the Phase-3 QA gates are recorded, which they now are — so
   `implemented`, optionally straight to `verified` given QA/verify/review/security all passed clean
   this cycle.)
2. **Amend the 002 entry wording** (D1: drop "explicit default"; D2: note the pinned MiniJinja
   feature-set) via `/speckit.roadmap.write` — housekeeping, non-blocking.
3. Apply via `/speckit.roadmap.write` (this debrief is read-only).
