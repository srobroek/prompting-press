# Task T025–T027 Report — Polish (rustdoc, gates, SC reconciliation)

**Date**: 2026-06-26 · T025 by rust-pro; T026/T027 run inline by the orchestrator.

## T025 — Crate rustdoc
`crates/prompting-press/src/lib.rs` crate-level docs cover all 8 required points: the four capabilities,
the C-06 normalization boundary (+ closed `code` vocabulary), the C-01 wraps-the-kernel stance, the
C-03 boundary (no I/O, output_model metadata-only, no token counting — F4), the three-sets invariant
(E1), the `check()` `meta.guard` provenance convention, a `check()` CI-usage doctest (P2), and a render
doctest. Both doctests runnable + passing. "roadmap decision C-NN" phrasing throughout (no
"constitution C-NN"). Plus a follow-up doc-link cleanup: the strict
`RUSTDOCFLAGS="-D warnings" cargo doc -p prompting-press --no-deps` build is now CLEAN (0 errors) —
fixed broken/ambiguous intra-doc links (KernelError full path, BTreeMap/BTreeSet qualified, check()/
render() function-vs-module disambiguation) across error/registry/check/compose. (Note: the strict doc
build is not a project CI gate — T026 gates `cargo test --doc` + clippy + fmt — but it's clean now.)

## T026 — Full local gate suite

| Gate | Result |
|------|--------|
| `moon run :build` (workspace) | ✅ GREEN |
| `cargo test -p prompting-press` | ✅ 36 (34 unit/integration + 2 doctests) |
| `cargo test --workspace` | ✅ 86 (kernel 50 + consumer 36 — no regressions) |
| `cargo clippy --workspace --all-targets -- -D warnings` | ✅ clean |
| `cargo fmt --check` | ✅ clean |
| `ci:check-ffi` | ✅ GREEN (consumer FFI-free with garde + serde_yaml_ng) |
| `ci:check-floating-versions` | ✅ GREEN |
| `ci:check-advisories` (cargo-deny) | ✅ GREEN |
| `schemas:codegen-check` | ✅ GREEN (no codegen impact) |

## T027 — Success Criteria → backing test reconciliation (8 SCs)

| SC | Statement (abbrev) | Backing test(s) | Status |
|----|--------------------|-----------------|--------|
| SC-001 | typed Vars + validate + render in one idiomatic call, no native types reached | render.rs V1.1 + render_validation.rs V1.4 | ✅ |
| SC-002 | invalid inputs rejected before render, structured error naming fields | render_validation.rs V1.2/V1.3 | ✅ |
| SC-003 | YAML ≡ JSON ≡ object identical representation | loader.rs V2.2 (serde_json::Value structural compare, 2 fixtures) | ✅ |
| SC-004 | agreement check detects undeclared var; passes clean; mutates nothing | check.rs V3.2/V3.5 + check_purity.rs V3.4 | ✅ |
| SC-005 | provenance lint flags untrusted/external declared with no guard configured | check.rs V3.3 (reframed F1) | ✅ |
| SC-006 | no native error type (garde Report / KernelError) on the public API | render_validation.rs V1.4 + error.rs unit tests | ✅ |
| SC-007 | zero pyo3/napi/FFI; no kernel-logic duplication | ci:check-ffi gate (T002, T026) + cargo tree | ✅ |
| SC-008 | N-entry composition → N ordered {role,text} messages, each with its own validated vars | compose.rs V4.1 | ✅ |
| ~~SC-009~~ | ~~token-count hook~~ | DROPPED (F4 — deferred to a later spec) | n/a |

**All 8 live Success Criteria have ≥1 passing backing test.** Quickstart V1.1–V4.4 + V5.3 (FFI) all
map to passing tests across the 6 consumer test files (V5.1/V5.2 removed with the token hook).

## Verdict

Clean. 86 workspace tests green, 8/8 CI-style gates green, strict doc build clean. Every live SC backed
by a passing test. Ready for Phase-3 QA (verify-tasks → verify → review → …).
