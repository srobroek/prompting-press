# Phase 1 (Setup) — T001 + T002 Report

**Branch:** 003-rust-consumer
**Date:** 2026-06-26

---

## T001 — Cargo.toml diff

File: `crates/prompting-press/Cargo.toml`

```diff
 [dependencies]
 prompting-press-core = { path = "../prompting-press-core" }
-# Required by the code-generated shape module (src/generated/prompt_definition.rs),
-# emitted from the JSON Schema by cargo-typify. serde derives + serde_json open-object
-# maps (meta/metadata). FFI-free, so this stays within the no-pyo3/no-napi rule (C-02).
+# serde/serde_json: dual-input loader (JSON arm) + the serialize bridge
+# (Value::from_serialize at the validated-struct → kernel-value boundary).
+# PromptDefinition shape is re-exported from prompting-press-core (relocated in spec 002).
+# FFI-free; stays within the no-pyo3/no-napi rule (C-02).
 serde = { workspace = true }
 serde_json = { workspace = true }
+# minijinja: consumer crate names minijinja::Value explicitly at the render boundary
+# (validated-struct → kernel-value bridge via Value::from_serialize).
+# The kernel already depends on minijinja; we reference the same workspace pin here
+# so the consumer can name Value without re-declaring a separate version.
+minijinja = { workspace = true }
+# garde: native Rust validation framework for typed Vars (constitution Principle VI).
+# features = ["derive", "serde"] — derive macros + serde integration; no extra validators.
+garde = { version = "0.23", features = ["derive", "serde"] }
+# serde_yaml_ng: YAML arm of the dual-input loader (Principle VII).
+# serde_yaml is archived; serde_yaml_ng is the maintained successor.
+serde_yaml_ng = "0.10"
```

Root `Cargo.toml` (workspace manifest): **no changes** — `minijinja` workspace pin was already present; garde and serde_yaml_ng are consumer-only and declared per-crate.

---

## T002 — Verification output

### cargo build -p prompting-press

```
Locking 13 packages to latest compatible versions
  Adding castaway v0.2.4
  Adding compact_str v0.9.1
  Adding equivalent v1.0.2
  Adding garde v0.23.0
  Adding garde_derive v0.23.0
  Adding hashbrown v0.17.1
  Adding indexmap v2.14.0
  Adding rustversion v1.0.22
  Adding ryu v1.0.23
  Adding serde_yaml_ng v0.10.0
  Adding smallvec v1.15.2
  Adding static_assertions v1.1.0
  Adding unsafe-libyaml v0.2.11
...
   Compiling prompting-press v0.0.0 (.../crates/prompting-press)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 18.07s
```

**PASS**

### moon run ci:check-ffi

```
FFI-isolation gate PASSED.
  prompting-press-core: no pyo3, no napi in dependency tree
  prompting-press: no pyo3, no napi in dependency tree
Tasks: 1 completed
 Time: 5s 614ms
```

**PASS**

### cargo tree -p prompting-press -i pyo3

```
error: package ID specification `pyo3` did not match any packages
```

**PASS — pyo3 absent**

### cargo tree -p prompting-press -i napi

```
error: package ID specification `napi` did not match any packages
```

**PASS — napi absent**

### moon run ci:check-floating-versions

```
Floating-version lint PASSED — all manifests use pinned versions.
  OK: mise.toml
  OK: Cargo.toml
  OK: packages/typescript/package.json
  OK: packages/python/pyproject.toml
  OK: crates/prompting-press/Cargo.toml
  OK: crates/prompting-press-core/Cargo.toml
  OK: crates/prompting-press-py/Cargo.toml
  OK: crates/prompting-press-node/Cargo.toml
Tasks: 1 completed
 Time: 14s 39ms
```

**PASS**

---

## Summary

| Gate | Result |
|---|---|
| `cargo build -p prompting-press` | PASS |
| `ci:check-ffi` | PASS |
| `cargo tree -i pyo3` | PASS (absent) |
| `cargo tree -i napi` | PASS (absent) |
| `ci:check-floating-versions` | PASS |
