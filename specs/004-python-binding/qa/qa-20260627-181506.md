# QA Report — Spec 004 (Python binding `prompting-press-py`)

**Date**: 2026-06-27 18:15 · **Mode**: CLI QA (Python/Rust library — not a web app)
**Verdict**: ✅ **ALL PASSED**

> Evidence: acceptance scenarios run against the freshly-built `cp310-abi3` wheel installed in an
> isolated venv (`/tmp/pp-verify-venv`). Raw transcript: `qa/responses/acceptance-scenarios.txt`.

## QA Summary

All 10 acceptance scenarios derived from `quickstart.md` (US1–US4 + boundary SCs) pass end-to-end against
the installed wheel. Both existing test suites pass (pytest 56, cargo 30). No blockers.

## Test Plan & Results (scenario-based, CLI mode)

| TC | User story / SC | Scenario | Result |
|----|-----------------|----------|--------|
| TC-001 | US1 / SC-001 | valid render → exact text + 64-hex template_hash & render_hash, variant=`default` | ✅ PASS |
| TC-002 | US1 / SC-002 | invalid `count=-1` → `PromptValidationError` naming `count` (code `validation`), no render | ✅ PASS |
| TC-003 | US2 / SC-003 | YAML / JSON / object → identical text AND identical both hashes | ✅ PASS |
| TC-004 | US2 / SC-003 | malformed YAML → `LoadError`, nothing loaded | ✅ PASS |
| TC-005 | US3 / SC-004,005 | `check()` flags `undeclared_variable` + `untrusted_without_guard` | ✅ PASS |
| TC-006 | US3 / FR-019 | clean registry → `passed()`/`is_empty()`; render hash unchanged before/after check (purity) | ✅ PASS |
| TC-007 | US4 / SC-008 | `from_messages` of 2 → exactly 2 ordered `{role,text}` (`system`,`user`) | ✅ PASS |
| TC-008 | US4 | invalid entry → exception, no partial; empty composition → `[]` | ✅ PASS |
| TC-009 | SC-006 | public error is a `PromptingPressError`, NOT `pydantic.ValidationError` | ✅ PASS |
| TC-010 | SEC-004 | secret in a value triggering a real kernel render error never leaks to `str`/rows; code `render` | ✅ PASS |

## Acceptance Criteria Coverage

| SC | Covered by | Status |
|----|-----------|--------|
| SC-001 render path | TC-001 + pytest test_render | ✅ |
| SC-002 reject-before-render, fields named | TC-002 + pytest (single + multi-field) | ✅ |
| SC-003 YAML/JSON/object parity | TC-003/004 + pytest test_loader | ✅ |
| SC-004 undeclared-variable | TC-005 + pytest test_check | ✅ |
| SC-005 untrusted-without-guard | TC-005 + pytest test_check | ✅ |
| SC-006 no native error type | TC-009 + pytest | ✅ |
| SC-007 pyo3 only in -py / no engine logic | `ci:check-ffi` (green, --force) | ✅ |
| SC-008 N ordered messages | TC-007/008 + pytest test_compose | ✅ |
| SC-009 abi3 wheel + fresh-env import | wheel built + installed in clean venv; import + render exercised | ✅ |
| SC-010 codegen-fresh + no token surface | `schemas:codegen-check` green; token grep empty; pytest no-token assertion | ✅ |
| SC-011 Python advisory gate | `ci:check-advisories-py` (green, prior gate run) | ✅ |

## Test Suite Results

- `pytest packages/python/tests` → **56 passed** (against the freshly-built wheel).
- `cargo test -p prompting-press-py` → **30 passed**.
- `cargo clippy -p prompting-press-py --all-targets -- -D warnings` → clean; `cargo fmt --check` → clean.
- `moon run ci:check-ffi --force`, `schemas:codegen-check --force` → green.

No pre-existing failures; no new failures.

## Environment

- OS: macOS (darwin 25.3.0, aarch64). Rust 1.95.0, Python 3.10 (verify venv) / 3.14.4 (dev), maturin 1.14.1, pydantic 2.x.
- Wheel under test: `target/wheels/prompting_press-0.0.0-cp310-abi3-macosx_11_0_arm64.whl` (built 18:10, contains the review-fix commit `0e537c8`).

## Metrics

- Scenarios: 10 total — 10 passed, 0 failed, 0 partial, 0 skipped.
- Suites: 86 tests (56 pytest + 30 cargo), all passing.

## Minor observations (non-blocking)

- **quickstart.md doc nit**: the US1 invalid-input example uses dict-style row access
  (`row["field"]`, `row["code"]`) but the implementation exposes attribute access (`row.field`,
  `row.code` — `FieldError` is a frozen pyclass). The doc example would `TypeError` if pasted. Cosmetic;
  candidate for the cleanup/docs step. Code is correct; tests + scenarios use the real `.field` API.

## Verdict

✅ **ALL PASSED** — proceed to `/speckit.code-review` + `/speckit.security-review` (parallel).
