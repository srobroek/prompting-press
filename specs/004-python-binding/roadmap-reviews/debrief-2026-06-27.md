# Post-Implementation Debrief Report: Spec 004 — Python binding (`prompting-press-py`)

**Date**: 2026-06-27
**Feature**: [specs/004-python-binding/spec.md](../spec.md)
**Roadmap**: [.specify/memory/roadmap.md](../../../.specify/memory/roadmap.md) — entry **004** (`[status: planned]`)
**Verdict**: ✅ PROCEED

---

## Summary

Spec 004 (the Python binding) was implemented and taken through the full Phase-3 QA suite. Checked the
built artifacts against the roadmap 004 entry's **outcome** and **scope (in/out)** and its governing
decisions (C-02, C-06). The outcome is **fully delivered**, every scope-in item maps to real code, no
scope-out boundary was crossed, and both governing decisions are upheld. No 🎯 Must-Address findings —
the entry is ready to be marked `verified`.

---

## Surfaced Context *(roadmap entry's recorded intent)*

- **Entry**: 004 — Python binding (`prompting-press-py` → `packages/python`) · **Status**: planned
- **Description**: PyO3 + Pydantic binding — consumer #1's language (Bellwether/`claudebroker`).
- **Outcome (expected)**: a `pip install` package — PyO3 marshaling over the kernel (maturin wheel), a
  Pydantic Vars facade with custom validators, the agreement + provenance lint wired to Pydantic fields,
  `from_messages` composition, and normalized errors as Python exceptions.
- **Scope (in)**: PyO3 marshaling (facade only); Pydantic Vars + validators; agreement/lint wiring;
  dual-input loader; `from_messages` composition; error normalization; a Python dependency advisory gate
  (`ci:check-advisories-py`, SEC-101).
- **Scope (out)**: any rendering/hashing/analysis logic in the binding (C-02); ~~token hook~~ (struck, F4).
- **Depends on**: 002 (kernel, implemented); informed by 003 (consumer, implemented).
- **Governed by**: C-02 (FFI isolation / zero engine logic), C-06 (per-language idiom + error normalization).

## Findings

| ID | Severity | Category | Finding | Suggestion |
|----|----------|----------|---------|------------|
| F1 | 🤔 | roadmap-stale (minor) | The 004 entry has no explicit follow-up note for the deferrals this cycle surfaced (TD001 depth-bound; D-1 `__eq__`/`__hash__`; D-2 `.pyi` stubs). The roadmap's WISHLIST/Deferred sections cover the variable-context render modes but not these binding-specific follow-ups. | Optionally record TD001/D-1/D-2 as a 004 follow-up note (or under 006/007) via `/speckit.roadmap.write`. Not blocking. |

**No outcome-miss, no scope-creep, no constraint-violation.**

### Outcome ↔ implementation map (all delivered)
- maturin abi3 wheel + `import prompting_press` → SC-009 (wheel built `cp310-abi3`, fresh-venv import verified).
- PyO3 marshaling (facade only) → `marshal.rs` `to_kernel_value`; render/check/compose delegate to core/consumer.
- Pydantic Vars facade + custom validators → `render.rs` validate-in-Python (`@field_validator` exercised in tests).
- Agreement + provenance lint wired → `check.rs` `check()` → `prompting_press::check` (SC-004/005).
- `from_messages` composition → `compose.rs` (SC-008; no `.chain()`).
- Normalized errors as Python exceptions → `error.rs` hierarchy `[{field,code,message}]` (SC-006).
- Python advisory gate → `scripts/ci/check-advisories-py.sh` + `ci:check-advisories-py` (SC-011).

### Scope-out compliance
- **Zero engine logic in the binding (C-02)**: verified — no minijinja Environment / Sha256 / agreement
  analysis; all delegated. `ci:check-ffi` green.
- **Token hook**: confirmed absent (F4); the roadmap scope line was reconciled by T027 (struck).

## Findings Summary

| Severity | Count |
|----------|-------|
| 🎯 Must-Address | 0 |
| 💡 Recommendation | 0 |
| 🤔 Question | 1 |

## Recommended Actions *(proposed only — never auto-applied)*

1. **Status transition**: propose **004 → `verified`** — outcome met, zero Must-Address findings. Apply
   via `/speckit.roadmap.write` (after the PR merges, matching the 003 cadence where the roadmap was set
   to `implemented`/`verified` at merge).
2. **F1 (optional)**: record the deferrals (TD001 depth-bound; D-1 value-equality; D-2 `.pyi` stubs) as a
   004 follow-up note or fold into 006/007 via `/speckit.roadmap.write`.

---

**Severity Legend**: 🎯 Must-Address · 💡 Recommendation · 🤔 Question. **Read-only report**; apply roadmap
changes via `/speckit.roadmap.write`.

*Generated by `/speckit.roadmap.debrief`.*
