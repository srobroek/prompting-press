# Post-Implementation Debrief Report: Spec 005 — TypeScript binding (`prompting-press-node`)

**Date**: 2026-06-28
**Feature**: [specs/005-ts-binding/spec.md](../spec.md)
**Roadmap**: [.specify/memory/roadmap.md](../../../.specify/memory/roadmap.md) — entry **005** (`[status: planned]`)
**Verdict**: ✅ PROCEED

---

## Summary

Spec 005 (the TypeScript binding) was implemented and taken through the full Phase-3 QA suite. Checked
the built artifacts against the roadmap 005 entry's **outcome** and **scope (in/out)** and its governing
decisions (C-02, C-06, + the new C-11). The outcome is **fully delivered**, every scope-in item maps to
real code, no scope-out boundary was crossed, and the governing decisions are upheld. One minor
**roadmap-stale** finding (the entry says "array-literal composition" but C-11 changed entries to
objects — the roadmap text lags the amendment, not the code). No 🎯 Must-Address — ready to mark
`implemented`.

## Surfaced Context *(roadmap entry's recorded intent)*

- **Entry**: 005 — TypeScript binding (`prompting-press-node` → `packages/typescript`) · **Status**: planned
- **Outcome (expected)**: an `npm i prompting-press` package — napi-rs marshaling over the kernel
  (platform-binary packaging), a Zod Vars facade with `.refine()` validators, agreement/lint wired to
  Zod, composition, and normalized errors as JS errors.
- **Scope (in)**: napi-rs marshaling; Zod Vars + validators; agreement/lint wiring; dual-input loader;
  composition; error normalization.
- **Scope (out)**: any engine logic in the binding; ~~token hook~~ (F4); a fluent `.chain()` API.
- **Depends on**: 002 (kernel, implemented); informed by 003/004 (both implemented).
- **Governed by**: C-02 (FFI isolation / zero engine logic), C-06 (per-language idiom + error
  normalization) — plus **C-11** (options objects), the amendment this spec's review produced.

## Findings

| ID | Severity | Category | Finding | Suggestion |
|----|----------|----------|---------|------------|
| F1 | 💡 | roadmap-stale | The 005 entry's `Scope (in)` says "**array-literal** / builder composition", but the C-11 amendment (made during this spec's review) changed composition entries to **options objects** `{ name, schema?, data, variant? }`. The code is right + C-11-conformant; the roadmap phrase lags. | Amend the 005 entry's composition phrase to "options-object entries" via `/speckit.roadmap.write`. Minor. |
| F2 | 🤔 | roadmap-stale (optional) | The 005 entry's `Governed by` lists C-02, C-06 but not **C-11** (which originated here and governs its call shape). | Add C-11 to the 005 `Governed by`. Optional bookkeeping. |

**No outcome-miss, no scope-creep, no constraint-violation.**

### Outcome ↔ implementation map (all delivered)
- `npm i` package + native addon → `packages/typescript` + `crates/prompting-press-node`; `pnpm pack`
  fresh-env import + render verified (SC-009); cross-language hash parity proven (TS == Python).
- napi-rs marshaling → `marshal.rs` (JS → serde_json → `from_serialize`); render/compose delegate to the kernel.
- Zod Vars facade + `.refine()` → `src/index.ts` `validateOrThrow` (SC-001/002).
- Agreement + provenance lint → `check.rs` → `prompting_press::check` (SC-004/005).
- Composition → `Composition` facade (objects, C-11); no `.chain()` (SC-008).
- Errors as JS errors → `PromptingPressError` hierarchy + `decodeAddonError` (SC-006).

### Scope-out compliance
- **Zero engine logic (C-02)**: verified — no minijinja Environment / Sha256 / agreement analysis in the
  binding; delegates to the shared core. `ci:check-ffi` green (asserts napi).
- **Token hook**: absent (F4). **`.chain()`**: absent + test-asserted (FR-013).

## Findings Summary

| Severity | Count |
|----------|-------|
| 🎯 Must-Address | 0 |
| 💡 Recommendation | 1 (F1) |
| 🤔 Question | 1 (F2) |

## Recommended Actions *(proposed only — never auto-applied)*

1. **Status transition**: propose **005 → `implemented`** — outcome met, zero Must-Address findings.
   Apply via `/speckit.roadmap.write` after the PR merges (matching the 002/003/004 cadence).
2. **F1 + F2**: amend the 005 entry — "array-literal" → "options-object" composition; add C-11 to
   `Governed by`. Fold into the same roadmap.write pass.
3. **Carry to the ledger** (this spec's deferred follow-ups): Python `py.typed`/`.pyi` (the one real
   capability gap), Rust `summarize` `write!`, Python `conftest.py` dedup, the TS `isSchema` doc-note;
   and the Python `render` keyword-only change already landed (`895a592`).

---

**Severity Legend**: 🎯 Must-Address · 💡 Recommendation · 🤔 Question. **Read-only report**; apply
roadmap changes via `/speckit.roadmap.write`.

*Generated by `/speckit.roadmap.debrief`.*
