# Retrospective — 006 Conformance corpus + cross-language hardening

**Date**: 2026-06-28 · **Cycle**: spec → clarify → plan → tasks → checklist → critique → security-review →
analyze → issues → implement → verify-tasks → verify → review → qa → code-review → security-review →
cleanup → memory → sync → retro. Single session, single author (agent + user gating).

## 📊 Development Cycle Metrics

```
Feature:           006 — Conformance corpus + cross-language hardening
Duration:          2026-06-28 13:54 → 14:46 (impl); full cycle within one day
Branch:            006-conformance-corpus (2 commits: f79782b spec checkpoint, 8919130 impl)

📝 Specification
Requirements:      19 FR + 7 SC = 26 total; 26 fulfilled, 0 partial, 0 dropped
Spec Accuracy:     100% (26/26)

📋 Planning
Tasks:             21 total, 21 completed; 0 added during impl (no converge needed)
Plan Score:        EXCELLENT — every task mapped to a real artifact; DAG ordering held

💻 Implementation
Commits:           2 (1 docs checkpoint + 1 impl)
Files changed:     39 (incl. spec artifacts); 22 code/config; 8 code/test + 8 fixtures
Lines:             +3774 / -1
Test/Code ratio:   the deliverable IS tests — 8 runner/support files + 8 fixtures + CI glue

🔍 Quality
Review findings:   🔴0 blockers 🟡0 important 🟢2 suggestions (1 fixed, 1 noted)
Security:          LOW (plan + branch); 0 Critical/High/Medium/Low; SEC-001/002 implemented
QA pass rate:      100% (6/6 scenarios; 7/7 SC; regression: Rust + Py 73 + TS 75 all green)
verify-tasks:      21/21 VERIFIED, 0 phantom
Quality Score:     EXCELLENT
```

## Specification Accuracy

100%. All 19 FR + 7 SC implemented with code evidence (verify-report.md). **One requirement was corrected
pre-implementation, not dropped:** the critique caught that the int-vs-float case as written (`1 vs 1.0`)
was physically unconstructable in JS; it was redefined to `1` vs `2.5` with `1.0` documented as excluded.
No scope creep, no deferred requirements. The two plan-time deferrals (exact serialized representations,
CI job topology) were execution details that changed no acceptance criterion.

## Plan Effectiveness — EXCELLENT

- **Architecture validated:** shared corpus + 3 thin runners + cross-check-via-golden worked exactly as
  planned. The Rust `RawVars` passthrough newtype (research D4) compiled and ran first try.
- **Two plan-time research findings proved load-bearing and correct:** (a) reading the marshaling source
  predicted the date/decimal canonical-form issue; (b) the plan caught that `cargo test -p prompting-press`
  ran nowhere in CI — the Rust conformance leg closed that gap (FR-015).
- **Zero tasks added during implementation** — the task DAG (foundational → US1 ∥ US2 → US3 → polish) held;
  no converge cycle.
- **Task scoping accurate** — the only re-sequencing was my choice to run the interdependent foundational
  chain main-thread rather than via worktree-isolated agents (recorded deviation).

## Implementation Quality — EXCELLENT

Clean across every gate: verify-tasks (0 phantom), verify (0 findings), review.run (2 LOW), code-review
(0 ≥80-confidence), security ×2 (LOW), qa (6/6 + zero regressions), cleanup (1 small fixed). The corpus
*earned its keep during its own construction* — it surfaced the loader-vs-schema-validator layer
distinction (memory A1) before that could become a latent wrong assumption.

## What Went Well (Keep Doing)

1. **The critique gate caught a real, load-bearing bug** (`1 vs 1.0` unconstructable in JS) before any
   fixture was authored — verified two ways (binding source + live `node`). This is the single highest-ROI
   moment of the cycle; the dual-lens critique paid for itself.
2. **Verify-at-source discipline** (reading the actual marshaling code, not trusting a subagent's summary)
   predicted the date/decimal divergence at plan time — so when both binding agents independently hit it,
   it was an expected, documented outcome, not a scramble.
3. **Delegation where it fit, main-thread where it mattered.** The idiom-specific runner work (Python/TS)
   delegated cleanly to specialists; the interdependent golden chain + all verification stayed main-thread
   where the fabrication risk and commit discipline were highest. Every delegated diff was re-run before
   acceptance.
4. **The corpus is genuinely adversarial** — the seeded-divergence test (proven to fail all three bindings,
   naming case+kind) means a green gate actually means something. The TS `assertDateDiverges()` self-check
   is a standout.
5. **APM-drift discipline held** across 2 commits — the 7 recompile-noise files never entered a feature
   commit despite ~10 commit-adjacent operations.

## What Could Improve (Start/Stop)

1. **The subagent tool-channel glitch (`tool_uses:0`) recurred and cost real time** — verify-tasks glitched
   twice, and review/security/sync subagents were unreliable enough that I ran them main-thread. This is a
   known issue (`memory/speckit-workflow-gotchas`) but it materially shaped execution this cycle.
2. **Repo-referenced scripts that don't exist** — `detect-changed-files.sh` (referenced by review.code and
   security-review) is absent; the sync steps are subagent-only with no skill. I worked around both, but a
   first-time runner would stall. (Also: the `rm-rf-guard.sh` hook has a syntax error that blocks legitimate
   `rm -rf` — flagged by the Python agent too.)
3. **memory-md / flash-mem MCP tooling absent this session** — capture, plan-with-memory, and both
   security reviews all assume MCP tools + a SQLite cache that weren't present; I used documented fallbacks
   every time, but the workflow assumes infrastructure that wasn't wired.

## Actionable Improvement Suggestions

```
IMP-001 [HIGH] Make Phase-3 read-only gates robust to the subagent channel glitch.
→ verify-tasks/review/security/sync subagents returned tool_uses:0 repeatedly this cycle.
→ Suggestion: the workflow steering should explicitly bless a main-thread fallback for the
  read-only Phase-3 gates (it already says "re-verify load-bearing findings main-thread"); make
  that the documented primary path until the glitch is fixed, rather than an exception.

IMP-002 [MEDIUM] Reconcile referenced-but-absent scripts/skills.
→ detect-changed-files.sh is referenced by 2 review skills but does not exist; sync.analyze/
  sync.conflicts are subagent-only. Fix the rm-rf-guard.sh syntax error.
→ Suggestion: either ship the scripts or update the skill text to a git-diff fallback.

IMP-003 [LOW] Spec data-model tables should be marked "design-time, confirm at impl" for
  empirically-determined values.
→ data-model.md's date/decimal construction + the variant-named-default verdict were both wrong
  at design time and corrected at sync. No harm (sync caught them) but the drift was predictable.
→ Suggestion: for cross-language serialization specifics, the spec should say "as-built TBD" rather
  than guessing a native construction.
```

## Historical Trend (vs prior specs)

No prior retro files in this feature dir; comparing to the as-built memories: specs 004/005 each shipped
clean with deferred DX follow-ups. 006 continued the streak (100% spec accuracy, clean gates) and is the
first to produce durable `docs/memory/` captures (D1, A1) — a sign the project is now generating reusable
architectural knowledge, not just feature narrative.

## Constitution Update — none proposed

This cycle surfaced no new *principle*. The two durable learnings (canonical-form marshaling, loader-vs-
schema layers) are implementation-level decisions correctly captured in `docs/memory/` (D1, A1), not
constitution-level amendments. The constitution stays at v1.1.0. (Offered per the skill; my recommendation
is no change — confirm if you disagree.)

## Next Actions

1. Final checkpoint commit (step 19) — the cleanup edit + spec doc-sync + memory + reports.
2. Open the PR for human review (the feature is verified across verify-tasks/verify/review/qa/security).
3. After merge: roadmap debrief → mark spec 006 `[status: implemented]` → spec 007 (v1 release) is next.
```
