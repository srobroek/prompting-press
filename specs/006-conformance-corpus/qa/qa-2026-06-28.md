# QA Report — 006 Conformance corpus + cross-language hardening

**Date**: 2026-06-28 · **Mode**: CLI QA (test corpus + CI gate; no web app) · **Verdict**: ✅ ALL PASSED
**Evidence**: `specs/006-conformance-corpus/qa/responses/` (gate-run.txt, seeded-divergence.txt, scope-guards.txt)

## QA Summary

✅ **ALL PASSED.** Every acceptance criterion (SC-001…SC-007) validated against actual execution; the full
conformance gate is green across all three bindings; the seeded-divergence negative test fails as designed;
all scope guards hold; and the full existing test suites pass with **zero regressions** (the corpus
additions broke nothing pre-existing).

## QA Test Plan & Results

| ID | User story / SC | Scenario | Mode | Result | Evidence |
|----|----|----|----|----|----|
| TC-001 | US1 / SC-001,002 | All 5 marshaling fixtures render to golden in all 3 bindings | CLI | ✅ PASS | gate-run.txt (Rust 2, Py 17, TS 16) |
| TC-002 | US2 / SC-003 | All schema fixtures reach the same verdict via each binding's loader (incl. YAML) | CLI | ✅ PASS | gate-run.txt |
| TC-003 | US3 / SC-005,007 | `moon run ci:conformance` runs all 3 legs green, locally reproducible | CLI | ✅ PASS | gate-run.txt ("Conformance gate PASSED") |
| TC-004 | US1 / SC-004 | Seeded divergence (render_hash=0×64) fails the runners, naming case+kind | CLI | ✅ PASS | seeded-divergence.txt (Rust + Python both FAIL; restored) |
| TC-005 | — / SC-006 | Scope guards: check-ffi green, spec-002 render fixtures byte-unchanged | CLI | ✅ PASS | scope-guards.txt ("FFI-isolation gate PASSED") |
| TC-006 | regression | Full existing test suites pass (no new failures) | CLI | ✅ PASS | see Test Suite Results |

## Acceptance Criteria Coverage

| SC | Status | Evidence |
|----|----|----|
| SC-001 100% marshaling parity | ✅ | TC-001 — all 3 bindings == shared golden |
| SC-002 all 5 hard cases | ✅ | TC-001 — date/decimal/nested/null/int-vs-float |
| SC-003 100% schema verdict parity + structured reject | ✅ | TC-002 — incl. YAML twins |
| SC-004 seeded divergence fails + names | ✅ | TC-004 — Rust+Py fail, naming case=decimal divergence=render_hash |
| SC-005 locally reproducible | ✅ | TC-003 — `moon run ci:conformance` |
| SC-006 no render-parity/engine-logic; spec-002 unchanged | ✅ | TC-005 — check-ffi PASSED; render fixtures unchanged |
| SC-007 gate enforced on PRs | ✅ | ci.yml `conformance` job (push/pull_request) |

## Test Suite Results (regression — step 8)

| Suite | Result | Note |
|----|----|----|
| `cargo test -p prompting-press -p prompting-press-core` | ✅ all pass | incl. the 3 new conformance test binaries; no failures workspace-wide |
| `pytest packages/python/tests` | ✅ **73 passed** | 56 prior + 17 new conformance — zero regressions |
| `node --test packages/typescript/test/*.test.mjs` | ✅ **75 pass, 0 fail** | 59 prior + 16 new conformance — zero regressions |

## Failures Detail

None. (TC-004 is a *negative* test: the failure it induces is the expected, designed behavior — the gate
correctly detecting a seeded divergence — and the fixture was restored to its correct golden immediately
after, verified.)

## Environment Info

- OS: darwin 25.3.0 · Toolchain via mise (Rust 1.95.0, Python 3.10+ in hermetic venv, Node 20+, moon)
- No application server (CLI feature); evidence is captured test-runner output under `qa/responses/`.

## Metrics

- Scenarios: 6 total — **6 passed**, 0 failed, 0 partial, 0 skipped
- Acceptance criteria: **7/7 SC validated**
- Regression suites: 3/3 clean (Rust workspace + Python 73 + TS 75)

## QA Verdict

✅ **ALL PASSED** — no blockers. Proceed to the code-review + security-review pass (steps 12/13).
