{
  "name": "@prompting-press-samples/greeter-cli",
  "version": "0.0.0",
  "description": "Greeter CLI — TypeScript consumer sample app for Prompting Press (spec 014, WU-C, T015/T016).",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "test": "pnpm run build && node --test \"test/*.test.mjs\"",
    "test:typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "prompting-press": "file:../../../packages/typescript",
    "zod": "^4.4.3"
  }
}
